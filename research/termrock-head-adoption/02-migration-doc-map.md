# 02 — Migration-doc map: which of 0028…0331 apply to jackin

Vetted: 2026-08-19

Questions: Which upstream migration docs apply to APIs jackin uses; what does each require?
Informs: termrock-migration
Method: codebase read — /Users/donbeave/Projects/tailrocks/termrock @ e1d61f4d67ea6f0f3adee578caa2c5dba642217e (https://github.com/tailrocks/termrock, branch main, clean tree) + rg verification in the jackin repo (/Users/donbeave/Projects/jackin-project/jackin, main @ c9be126c). All migration-doc paths below are relative to the TermRock clone; all crate paths are relative to the jackin repo root. Doc citations use `migrations/<file>:<line>` at commit e1d61f4d.

Baseline: jackin pins `termrock = "=0.11.0"` at git rev `5ff94ee117fd4a1b72fdd0d1b1847815055a93ac`, features `["crossterm", "serde"]` — `Cargo.toml:118`, `Cargo.lock:6684-6686`. Six crates import termrock: `jackin`, `jackin-capsule`, `jackin-console`, `jackin-launch`, `jackin-oppicker`, `jackin-tui` (grep of `crates/*/Cargo.toml`). Doc 0331 names exactly this pin rev as the migration origin (`migrations/0331-v0.14.0-jackin-parity-handoff.md:12-13`). HIGH.

`migrations/` holds 331 numbered docs; the assigned range 0028–0331 contains 304 (method: `ls migrations/ | wc -l` = 331, minus 0001–0027). HIGH.

## Findings

### Applicable docs

Every row read in full from the primary source (HIGH confidence unless noted). "Affected crates" verified by rg in jackin; one citation per API given (more exist — counts are total rg hits across the workspace, method: `rg -c <pattern> --type rust`, 2026-08-19).

| Doc | Title | What changes | jackin APIs affected | Affected crates (evidence) |
|---|---|---|---|---|
| 0060 | Crate root re-export purge | All root `pub use` deleted; types only at module paths (`termrock::style::Theme`, `interaction::…`) (`migrations/0060…md:5-33`) | Every root-path import: `termrock::Theme::default()` (351 hits), `use termrock::{Theme, …}` | all 6 — e.g. `crates/jackin-tui/src/operator_info.rs:414`, `crates/jackin-capsule/src/tui/ansi.rs:5` |
| 0061 | DesignSystem sole paint | Hard break, "No deprecated aliases": `Theme`→`style::RolePalette`, `DesignTokens`→`DesignSystem`, `PanelEmphasis`→`style::PanelChrome`, all widget ctors take `&DesignSystem`, `quantize_theme`→`quantize_palette`, `theme_for_appearance`→`palette_for_appearance` (`migrations/0061…md:3-17`). Verified at HEAD: gate test asserts `Theme`/`PanelEmphasis` absent (`crates/termrock/src/lib.rs:123,139`); `RolePalette` at `crates/termrock/src/style/mod.rs:355`; `render_dialog_shell(frame, area, title, PanelChrome, &DesignSystem)` (`crates/termrock/src/layout/dialog.rs:15-21`); `Toast::new(&DesignSystem, …)` (`crates/termrock/src/widgets/toast.rs:1313`) | `Theme::default()` (351), `Theme::with_role`, `PanelEmphasis` (63), every `&theme` widget arg: `Panel::new`, `StatusBar::new`, `Tabs::new`, `Toast::new`, `Viewport::new`, `render_dialog_shell`, `render_scrollable_dialog_body`, `render_hint_bar` | all 6 — `crates/jackin-capsule/src/tui/components/chrome.rs:250`, `crates/jackin-launch/src/tui/run.rs:987` (`with_role`), `crates/jackin-launch/src/tui/components/prompts.rs:356` (`Panel::new(&theme)`), `crates/jackin/src/console/adapter/run.rs:445` (`StatusBar::new(…, &theme)`), `crates/jackin-capsule/src/tui/components/dialog_widgets.rs:560` (`render_dialog_shell`), `crates/jackin-launch/src/tui/view.rs:78` (`render_hint_bar`) |
| 0062 | InteractionScene sole focus | Public `FocusRing`/`FocusTarget`/`FocusOutcome` deleted; collection `set_focused` deleted; paint via `Widget::focused(bool)`; host gates keys via scene (`migrations/0062…md:5-11`). Verified: HEAD `interaction/mod.rs` no longer re-exports `FocusRing` (gate `crates/termrock/src/lib.rs:66`; struct itself now crate-internal at `crates/termrock/src/interaction/focus.rs:35`) | `interaction::FocusRing` (6 hits) | jackin-tui — `crates/jackin-tui/src/runtime/focus.rs:6,20`, `crates/jackin-tui/src/runtime/modal_flow.rs:6,14`. jackin has no `ListState::set_focused` calls (rg: 0) |
| 0065 | OverlayStack sole overlay authority | Public `ModalStack`, `ModalClickResult`, `classify_click` deleted → `OverlayStack` + `handle_outside_click`; `render_backdrop` kept (`migrations/0065…md:5-12`). Verified: no `pub struct ModalStack` at HEAD; gate `crates/termrock/src/lib.rs:166` | `interaction::{ModalStack, ModalClickResult, classify_click}` | jackin-tui (`crates/jackin-tui/src/runtime/modal_flow.rs:13`), jackin-capsule (`crates/jackin-capsule/src/tui/components/dialog.rs:938-939`), jackin-console (`crates/jackin-console/src/tui/run.rs:436-452`) |
| 0034 | Unified InteractionScene | Removes `OverlayController`/`EscCascade`-family (jackin never imported them); establishes the scene lifecycle 0062/0065 point to; "TermRock never invents Quit" — host handles `UnhandledEscape` (`migrations/0034…md`) | replacement target for `FocusRing`/`ModalStack` code above | jackin-tui (same citations as 0062/0065) |
| 0080 | EventResult + typed outcomes | Additive envelope; per-widget `*Outcome` enums and `interaction::Outcome` preserved (`migrations/0080…md`, "Preserved"). Verified: `interaction::Outcome<T>` at HEAD `crates/termrock/src/interaction/mod.rs:126` | `interaction::Outcome` | jackin-launch — `crates/jackin-launch/src/tui/subscriptions.rs:13` |
| 0085 | ScrollArea canonical scrolling | `scroll::{DialogScroll, TailScroll, max_offset, apply_delta_u16, render_scrollbar, …}` all "Preserve"; new `ScrollAreaState` (jackin doesn't use it) (`migrations/0085…md`). Verified at HEAD: `is_scrollable` `crates/termrock/src/scroll/mod.rs:160`, `dialog_scroll_axes` `:214`, `scroll_hint_spans` `:227`, `viewport_width` re-export `:16-24` | `scroll::{ScrollAxes, DialogScroll, viewport_width, is_scrollable, scroll_hint_spans, dialog_scroll_axes}` (203 module refs) — no rename, only the `&theme` args of surrounding paint calls change (0061) | jackin-tui (`crates/jackin-tui/src/operator_info.rs:16,208`), jackin-console (`crates/jackin-console/src/tui/scroll_block.rs:20`), jackin-capsule, jackin-launch |
| 0110 | StatusBar premium regions | `StatusSlot` gains public fields `region`, `kind`, `glyph`, `style_explicit`; `StatusBarState` gains `transient`; doc migrates struct-literal construction to builders (`migrations/0110…md`, "Consumer edits" diff). Verified fields at HEAD `crates/termrock/src/widgets/status_bar.rs:171-194,357-363` | `StatusSlot { … }` and `StatusBarState { … }` struct literals stop compiling (missing fields) | jackin-capsule — `crates/jackin-capsule/src/tui/components/branch_context_bar.rs:146,253,269`; jackin — `crates/jackin/src/console/adapter/run.rs:400,429` |
| 0114 | Text semantic typography | `text::display_cols`/`take_display_cols` "Preserve — measurement kernel"; adds `Text`/`TextSpan`; directive to migrate ad hoc `set_stringn` + Role labels (`migrations/0114…md`) | `text::display_cols` (53 hits) — unchanged | jackin-console — `crates/jackin-console/src/tui/list_geometry.rs:184` |
| 0120 | Kbd and ShortcutHint | `chord_glyph`, `HintBar`/`HintSpan`, `Keymap::glyph_for/push_spans_for/hint_spans` all "Preserved"; adds `Kbd`/`ShortcutHint` (`migrations/0120…md`). Verified at HEAD: `chord_glyph` `crates/termrock/src/keymap.rs:595`, `glyph_for` `:348`, `hint_spans` `:273`, const submodule `keymap::glyph` `:566` | `keymap::{chord_glyph, glyph::*, Keymap::dispatch/hint_spans/glyph_for}` (18+ refs), `widgets::HintSpan` | jackin-capsule (`crates/jackin-capsule/src/tui/input.rs:370` dispatch), jackin-console (`crates/jackin-console/src/tui/components/footer_hints/common.rs:8` `keymap::glyph`), jackin-launch (`crates/jackin-launch/src/tui/keymap.rs:12` HintSpan) |
| 0121 | Link and ActionLink | `osc::encode_hyperlink_open`, `Role::Link/LinkHover`, DetailTable link rows all "Preserved"; adds Link widget (`migrations/0121…md`) | `osc::{encode_hyperlink_open, encode_hyperlink_close}` (4 hits) — unchanged | jackin-launch (`crates/jackin-launch/src/tui/components/failure_dialog.rs:352,362`), jackin-tui (`crates/jackin-tui/src/operator_info.rs:574`) |
| 0124 | AnsiText | `strip_bytes`/`styled_spans`/`line_from_ansi` "Preserved — now route through full parser" (behavioral upgrade) (`migrations/0124…md`). Verified: `line_from_ansi` at HEAD `crates/termrock/src/ansi_text.rs` | `ansi_text::line_from_ansi` (1 hit) | jackin-launch — `crates/jackin-launch/src/tui/components/build_log_dialog.rs:266` |
| 0125 | KeyValueList | `DetailTable`/`DetailRow`/`DetailCapability` "Preserved — selection-heavy dialog details"; adds KeyValueList as the lighter alternative (`migrations/0125…md`) | `widgets::{DetailTable, DetailRow, DetailCapability, DetailTableState, DetailTableOutcome}` (19+ hits) — unchanged | jackin-launch — `crates/jackin-launch/src/tui/components/failure_dialog.rs:17,144`, `crates/jackin-launch/src/tui/components/prompts.rs:241` |
| 0138 | TextInput production editor | `TextInputState`/`TextInputOutcome`/`EditAction::{Insert,Backspace,Delete}` preserved; `EditAction::Move*` become struct variants; `TextInputOutcome` gains `ClipboardCopy/ClipboardCut/ClipboardPasteRequest/Cleared` and is `#[non_exhaustive]` (`migrations/0138…md`; HEAD `crates/termrock/src/widgets/text_input.rs:148-173`) | `TextInputState` (157), `TextInputOutcome` (15), `EditAction::{Insert,Backspace}` — jackin uses no `Move*` variants (rg: only Insert/Backspace, `crates/jackin-capsule/src/tui/components/dialog/input.rs:51,67`). Existing wildcard match arms compile but silently drop the new clipboard outcomes (behavioral) | jackin-oppicker (`crates/jackin-oppicker/src/adapters.rs:39-42`), jackin-launch (`crates/jackin-launch/src/tui/run.rs:303-310`), jackin-console (`crates/jackin-console/src/tui/components/dialogs.rs:139`) |
| 0152 | Tabs | `TAB_GAP`, `lay_out_tabs`, `tab_at_column`, `Tab`/`Tabs`/`TabsState` names all "Preserved (fields expanded)" (`migrations/0152…md:14-18`). Verified: `TAB_GAP: u16 = 1` identical at pin and HEAD (`crates/termrock/src/widgets/tabs.rs:48`) | `widgets::{Tabs, TabsState, Tab, TAB_GAP, lay_out_tabs, tab_at_column}` — only the ctor's `&Theme` arg changes (0061) | jackin-console (`crates/jackin-console/src/tui/components/editor_rows.rs:220`, `crates/jackin-console/src/tui/layout.rs:206`), jackin-capsule (`crates/jackin-capsule/src/tui/components/status_bar.rs:10`) |
| 0168 | Dialog | `Dialog::new`/`Backdrop`/`MessageDialog` "Preserved"; `ChoiceDialog`/`ChoiceDialogState` "Migrated (wraps DialogState; same Outcome API)" (`migrations/0168…md:15-22`). Verified: `ChoiceDialog::gap(&str)` still at HEAD `crates/termrock/src/widgets/dialog.rs:1741` | `widgets::{Dialog, MessageDialog, ChoiceDialog, ChoiceDialogState, Backdrop}` | jackin-capsule (`crates/jackin-capsule/src/tui/components/dialog.rs:1052,1060`, `crates/jackin-capsule/src/tui/view.rs:204`), jackin-console (`crates/jackin-console/src/tui/view.rs:388`), jackin-launch (`crates/jackin-launch/src/tui/components/dialog.rs:13`) |
| 0174 | Toast | `Toast::new(system, msg, severity)`, `Severity`, `Anchor`, `ToastState` API "Preserved"; queue/stack added (`migrations/0174…md:14-21`) | `widgets::{Toast, Severity, Anchor}` — only paint-arg type changes (0061) | jackin-capsule — `crates/jackin-capsule/src/tui/view.rs:343-344` |
| 0177 | ProgressBar | `Progress`/`ProgressKind` "Preserved (`Progress` = `ProgressBar`)" (`migrations/0177…md:14-16`); verified export at HEAD `crates/termrock/src/widgets/mod.rs:697` | `widgets::{Progress, ProgressKind}` | jackin-launch — `crates/jackin-launch/src/tui/components/progress_rail.rs:44-45` |
| 0185 | List collection view | `List::new`, `ListRow` builders, `ListState` selection/offset/hover/click, `RowRole::Item/Separator` all "Preserved" (`migrations/0185…md:18-23`); verified `RowRole` at HEAD `crates/termrock/src/widgets/list.rs:97-101` | `widgets::{List, ListRow, ListState, RowRole}` (41+ hits) | jackin-console — `crates/jackin-console/src/tui/screens/workspaces/view.rs:862-873`, `crates/jackin-console/src/tui/components/github_picker.rs:153` |
| 0191 | KeyValueTable | `DetailTable`/`DetailRow` "Preserved — dialog selection/copy/link surface"; nothing deleted; KeyValueTable added as the flagship for new panels (`migrations/0191…md`) | `widgets::DetailTable` family — unchanged | same as 0125 |
| 0196 | DiffView | `DiffState` "Migrated — type alias of `DiffViewState`"; struct-literal `DiffLine { text, kind }` "Migrated — `DiffLine::new` / `::added` / `::removed`"; `DiffView` redesigned (unified/split/search/fold) (`migrations/0196…md:18-26`). Verified alias at HEAD `crates/termrock/src/widgets/diff.rs:562` | `widgets::{DiffView, DiffState, DiffLine, DiffKind}`; jackin builds the migrated struct literal | jackin-launch — `crates/jackin-launch/src/tui/run.rs:835,985` (`DiffLine { text, kind: *kind }`) |
| 0261 | Surface ladder + role expansion | `Role` gains 14 variants (`Raised`, `SelectionTint`, `ActorUser`, …), `ROLE_COUNT` 49→63; `Role` stays `#[non_exhaustive]` — "external matches must retain a wildcard"; phosphor paints an obsidian surface ladder instead of inheriting terminal background; `DesignSystem::terminal_native()` is the opt-out (`migrations/0261…md`) | all 18 distinct `style::Role` variants jackin uses survive (verified by name at HEAD `crates/termrock/src/style/mod.rs:134-196`; an earlier count of 20 was a grep artifact that also captured `RowRole::Item`/`RowRole::Separator`); background behavior of every jackin surface changes unless `terminal_native()` chosen | all TUI crates; e.g. `crates/jackin-capsule/src/tui/components/chrome.rs:204` |
| 0268 | Composition measurement | Adds `DetailTable::measure`, `layout::panel_stack` — additive sizing helpers for surfaces jackin composes by hand (`migrations/0268…md:14-31`) | optional adoption for `DetailTable`/dialog sizing | jackin-launch (`crates/jackin-launch/src/tui/components/prompts.rs:241`) |
| 0281 | Craft helpers | `CellAlignment`/`CellOverflow` move `widgets::table` → `text` (jackin: 0 uses, rg); right-aligned truncation now contracts from the head; ASCII ellipsis becomes 3-column `...`; `DesignSystem` struct literals need `kv_separator` field (`migrations/0281…md`) | no direct API hit; snapshot-visible truncation/ellipsis changes | jackin-capsule/jackin-console insta snapshots (both crates depend on insta, `crates/jackin-capsule/Cargo.toml`, `crates/jackin-console/Cargo.toml`) |
| 0284 | Graphite role ladder | `Role::TabUnderlineFocused/TabUnderlineUnfocused` removed (jackin: 0 uses, rg); `TextFaint`/`BackdropWash` added; positional role order changed — positional palettes and persisted `role as usize` must rebuild via `RolePalette::from_fn` (jackin: no positional palette, rg); whole default palette re-tiered (`migrations/0284…md`) | `Theme::default().with_role(…)` call sites must move to `RolePalette` construction; every themed color shifts | jackin-launch — `crates/jackin-launch/src/tui/run.rs:987-988` |
| 0291 | One scrollbar language | All scroll surfaces now paint `·` track + `┃`/`━` thumb from `Role::ScrollTrack`/`ScrollThumb`; "snapshots … pinning `█` or `│` … re-bless"; full-height thumb no longer painted when content fits (`migrations/0291…md`) | `Role::{ScrollTrack, ScrollThumb}` styling semantics; jackin components that mimic termrock scrollbars | jackin-launch (`crates/jackin-launch/src/tui/components/header.rs:32`), jackin-console (`crates/jackin-console/src/tui/screens/workspaces/view.rs` ScrollTrack uses) |
| 0316 | One token family | Adds `TabsActiveCue`, `FocusEmphasis`, `SurfaceFamily`, `keycap_text`; tabs' active-tab cue becomes a named policy (`migrations/0316…md`) | `widgets::Tabs` visual behavior; explicit cue selection available | jackin-console/jackin-capsule tab strips (citations as 0152) |
| 0323 | One modal geometry | Adds `layout::{ModalSpec, modal_rect}` as the sole modal-geometry authority; consumer directive: take the authority rather than hand-roll clamp math (`migrations/0323…md`). jackin's `layout::{resolve_dialog, DialogSpec, Placement, bottom_rows}` unchanged at HEAD (`crates/termrock/src/layout/mod.rs:95`) | optional adoption for jackin dialog placement | jackin-capsule — `crates/jackin-capsule/src/tui/components/dialog_widgets.rs:560` area math |
| 0327 + 0329 | Tabs focus-aware rule + selection wash | Behavioral: default active cue = bottom rule (`Role::Accent` focused / `Role::Border` unfocused) plus `SelectionTint` wash; "No consumer edit is required"; named part of jackin's visual contract by 0331 (`migrations/0327…md`, `migrations/0329…md`, `migrations/0331…md:47-48`) | tab strip appearance; snapshots | jackin-console/jackin-capsule (citations as 0152) |
| 0328 + 0330 | ChoiceDialog measured + centered actions | Behavioral: actions stay horizontal when measured labels fit, centered; hit regions only over painted labels; "No consumer edit is required"; named part of jackin's visual contract by 0331 (`migrations/0328…md`, `migrations/0330…md`, `migrations/0331…md:49-51`) | ChoiceDialog appearance; snapshots | jackin-capsule — `crates/jackin-capsule/src/tui/components/dialog.rs:1052-1060` |
| 0331 | Jackin parity handoff | Master doc: declares parity from rev `5ff94ee…`; theme replacement = `style::RolePalette` (normally `RolePalette::tailrocks_phosphor()`); adds `TerminalCellSource`/`TerminalCellGrid` and `ReadySubscription` publics (formerly host-local generics); PNG regression workflow (`mise run png-baselines` / `bless-pngs`, zero-tolerance decoded pixels) (`migrations/0331…md`) | whole migration; the two new publics map to jackin host-local terminal-grid and ready-subscription adapters | all 6 crates |

Second-pass additions (2026-08-19, after cross-checking chapter 01's measured breaks against this table — the original symbol-grep triage had seven false negatives):

| Doc | Title | What changes | jackin APIs affected | Crates / evidence |
|---|---|---|---|---|
| 0039 | Composed row and panel anatomy | `ListRow` gains `leading/secondary/badge/shortcut/loading`; prefer `ListRow::item`/`composed` (`migrations/0039…md:17,41-47`) | `ListRow` struct literals (5 of the 8 missing fields in ch01 finding 9) | `crates/jackin-console/src/tui/components/agent_choice.rs:110` |
| 0057 | Category-leading List redesign | "Struct literals remain valid but must include every field" (`migrations/0057…md:52`); fluent builders; `ListClickPolicy` makes click activation policy-driven (`:20`) | `ListRow` literals; `ListState` click behavior in pickers (MED on click-policy impact) | `crates/jackin-console/src/tui/components/file_browser/render.rs:138` |
| 0073 | Dialog/ChoiceDialog action cursor | `ChoiceDialogState.focused` → `cursor`, `ActionBarState.focused` → `cursor`; Tab leaves `handle_key`; `set_accepts_input` gating (`migrations/0073…md:5-10,29-39`) | `ChoiceDialogState`, `ActionBarState` — 7 measured compile errors (ch01 finding 11) | `crates/jackin-console/src/tui/components/dialogs.rs:231`, `confirm_save.rs:323` |
| 0083 | CollectionState headless collection model | `ListState` embeds `CollectionState`; "`ListState::new` is no longer const" (`migrations/0083…md:23,49`) → `for_count` non-const too (termrock `widgets/list.rs:321,358,928`) | `ListState::for_count` in a `const fn` — resolves ch01 finding 6's "no doc found" | `crates/jackin-oppicker/src/state.rs:265-267` |
| 0263 | Spacing activation | `DialogSpec` adds `preferred_reference_pct` + `preferred_pct_of_reference`; Panel/Dialog interior insets change (`migrations/0263…md:10-19`) | `layout::DialogSpec` literals — 3 measured compile errors (ch01 finding 12); dialog snapshot geometry | `crates/jackin-launch/src/tui/components/dialog.rs:22` |
| 0288 | One paint authority | `SelectionChrome` default `Fill` → `Gutter` (all list selection paint); surface ladder re-fills; `Backdrop::from_tokens` paints solid `BackdropWash`; `render_backdrop` gains `&system` arg; `DialogVariant::Info` drops forced focus chrome (`migrations/0288…md:15-21,109-124,140-142`) | `widgets::{List, Backdrop, Panel, Dialog}` appearance; no compile break (jackin never calls `render_backdrop` directly — rg 0). Behavioral, MED | `crates/jackin-console/src/tui/screens/workspaces/view.rs:873`, `crates/jackin-capsule/src/tui/view.rs:204` |
| 0298 | Status lives in the glyph | StatusBar slot words → `Role::StatusBar`, kind color moves to the glyph cell; `StatusSlotKind::default_role()` changed for Mode/Selection; explicit styles still win (`migrations/0298…md:20,36-38`) | semantics of the new `StatusSlot.glyph`/`style_explicit` fields (ch01 finding 7); status-bar snapshots | `crates/jackin-launch/src/tui/components/footer.rs:43`, `crates/jackin/src/console/adapter/run.rs:400` |

Cross-cutting mechanical deltas the table rows imply, verified at HEAD:

- Every widget/paint entry point takes `&DesignSystem` where jackin passes `&Theme` today — e.g. `render_hint_bar(frame, area, spans, system: &DesignSystem)` (`crates/termrock/src/widgets/hint_bar.rs:237-241`). jackin's 351 `Theme::default()` sites (method: `rg -c "Theme::default\(\)" --type rust`, summed) all feed such calls. HIGH.
- `HintSpan` is preserved as the same 6-variant enum (`crates/termrock/src/widgets/hint_bar.rs:18-31`); `wrapped_hint_lines` preserved (`:common` — `crates/termrock/src/widgets/hint_bar.rs`). HIGH.
- `input::{KeyEvent, KeyCode, KeyModifiers, MouseEvent, …}` and `keymap::{KeyChord, Keymap, KeyBinding}` unchanged as paths (`crates/termrock/src/input/mod.rs:5-9`; 0060 states widgets/input/keymap "already module-path — unchanged", `migrations/0060…md:35-36`). HIGH.
- `osc::{PointerShape, ClipboardWrite, ClipboardSelection, encode_pointer, encode_clipboard}` present at HEAD (`crates/termrock/src/osc/request.rs`, `crates/termrock/src/osc/encode.rs`); no 0028+ migration doc touches them besides 0121 ("Preserved"). jackin cites: `crates/jackin-capsule/src/tui/terminal.rs:12,77`, `crates/jackin-capsule/src/tui/view.rs:413-414` (direct `termrock::osc::` call sites; `daemon/mouse_input.rs` reaches `PointerShape` via the crate-local re-export at `model.rs:13`), `crates/jackin-tui/src/operator_info.rs:574`. HIGH.
- `StatusBar::alpha` and `StatusBar::regions` survive (`crates/termrock/src/widgets/status_bar.rs:503,574`) — jackin-launch `crates/jackin-launch/src/tui/components/footer.rs:73,200`. HIGH.
- termrock's crate features `crossterm`, `serde` unchanged at HEAD (`crates/termrock/Cargo.toml:17-20`). HIGH.

### Non-applicable classes

Counts by numeric range minus the docs listed above (method: filename arithmetic over `ls migrations/`; 304 total = **40 applicable** (33 first-pass + 7 second-pass) + 6 checked-not-applicable + **258** in classes below). Class membership judged from titles plus grep for jackin's API names inside the docs (`rg -l` over 30+ symbol patterns); the first-pass triage missed 7 docs later caught by cross-checking chapter 01's measured breaks and a wider symbol re-grep — class assignment for unread docs is MED, the counts themselves HIGH.

| Class | Range | Count | Why not applicable |
|---|---|---|---|
| v0.12 experience-layer / workbench / theme-foundation build-out | 0028–0059 minus {0031, 0034, 0047, 0039, 0057} | 27 | Internal foundations (virtual grid, transcript, agent workbench, prompt composer, design inspector, lookbook); consumer-visible outcomes restated harder by 0060–0065. jackin imports none of the added APIs (module-import survey: jackin uses only `widgets, scroll, keymap, layout, interaction, input, text, osc, style, ansi_text` + root `Theme`) |
| v0.13 internal cutovers, kernel additions, layout primitives | 0063–0109 minus {0065, 0080, 0085, 0073, 0083} | 42 | Lookbook/agent/form/menu "premium" passes and additive kernel (FocusGraph, RovingFocusGroup, CollectionState, Virtualizer, AppShell, Surface, Grid, …) — components/paths jackin never imports |
| v0.13 component catalog for unimported widgets | 0111–0256 minus {0114, 0120, 0121, 0124, 0125, 0138, 0152, 0168, 0174, 0177, 0185, 0191, 0196} | 133 | Badge, avatar, charts, form controls, pickers, palette, chat/agent components, blocks — none imported by jackin (verified against the full `use termrock::widgets::…` import inventory) |
| v0.13 recipe/pattern/design sweeps | 0257–0280 minus {0261, 0268, 0263} | 21 | `termrock::patterns`, lookbook recipes, frame-time portability — jackin imports no `patterns`, `perf`, `runtime`, `capability`, or `registry` items (rg: 0 hits for `termrock::(patterns|perf|runtime|capability|registry)`) |
| v0.14 paint-behavior sweeps, mostly no consumer edit | 0282–0330 minus {0284, 0291, 0316, 0323, 0327, 0328, 0329, 0330, 0288, 0298} and minus the 4 checked singles below | 35 | Behavioral paint refinements (contrast floor, motion, truncation honesty, microcopy, spinner channels, …). No API change for jackin; collective consequence is insta snapshot re-blessing in `jackin-capsule`/`jackin-console` (both use insta; TESTING.md's snapshot-review gate applies) |

Checked individually and found not applicable (HIGH, each read):

- 0031 — additive v0.12 kernel (`DesignTokens`, `UiIntent`, `SemanticScene`); superseded by 0060/0061/0062.
- 0047 — additive `data_view` kit; jackin imports none of it (rg: 0). Carries a directive (below).
- 0322 / 0324 — `termrock::patterns` workbench fixes/breaks only; jackin imports no patterns (rg: 0).
- 0325 — breaking `ToastState::advance/is_visible` signatures; jackin constructs `Toast` but has no `ToastState` (rg: 0 in jackin; `crates/jackin-capsule/src/tui/view.rs:343` is stateless render).
- 0326 — removes `widgets::ascii_ellipsis`, `BREADCRUMBS_ELLIPSIS*`; jackin imports none (rg: 0 — `crates/jackin/src/cli/usage/tests.rs:110` is jackin's own function name).

### Upstream consumer directives (data)

Reported as content found in the docs — these are upstream's imperatives to consumers, not this report's recommendations:

| Directive (verbatim or near-verbatim) | Source |
|---|---|
| "Migrate imports directly to the current modules; do not add compatibility facades." | `migrations/0331…md:18-19` |
| "replace the old root theme with `termrock::style::RolePalette`, normally `RolePalette::tailrocks_phosphor()`, and follow the earlier numbered migrations for renamed APIs" | `migrations/0331…md:19-22` |
| "Jackin product assemblies remain consumer-owned. BrandHeader, digital rain, launch animation, and other brand/domain compositions must not move into `termrock::widgets`." | `migrations/0331…md:36-39` |
| PNG workflow: `mise run png-baselines`; after intentional paint change inspect then `mise run bless-pngs`; "A platform-only mismatch with no intended paint change is a raster pipeline defect and must not be blessed." Historical side harness "an audit tool, not a runtime dependency or consumer migration requirement." | `migrations/0331…md:56-80` |
| "No deprecated aliases are provided. This is a hard break." | `migrations/0061…md:16` |
| Consumer edits 1–4: delete collection `set_focused`; only call `handle_key` when the surface owns scene focus; pass `.focused(bool)`; "Stop importing public `FocusRing`". Also: "Hosts that call `handle_key` on unfocused collections will process keys (by design)." | `migrations/0062…md` (Consumer edits, New constraints) |
| "Replace `ModalStack<T>` with `OverlayStack` + `Option<T>` domain payload. Register every floating UI via `stack.open`." Esc order: overlay before scene. | `migrations/0065…md` (Lookbook / hosts, Canonical replacement) |
| "On Esc, handle `UnhandledEscape` for quit — TermRock never invents Quit." | `migrations/0034…md` (Exact consumer edits #2) |
| StatusSlot: replace struct literals with `StatusSlot::new(id, content).priority(p).style(style)` or semantic builders | `migrations/0110…md` (Consumer edits diff) |
| "derive display from the active `Keymap` rather than hardcoded labels" / "Every frame: rebuild from live sources (never hardcode chords)" | `migrations/0120…md` (Mission), `migrations/0163…md` (Consumer edits) |
| "New large-table work should project only `VirtualWindow::visible_range()` rows. Do not build a full `Vec` of 1M row widgets." | `migrations/0047…md` (Consumer direction) |
| Positional palettes must be rebuilt via `RolePalette::from_fn`; "Code that persisted `role as usize` indices … must be rebuilt against `RolePalette::roles()`." | `migrations/0284…md` |
| "`Role` remains `#[non_exhaustive]`; external matches must retain a wildcard." Custom `ListRowRecipe` construction must populate `hover_wash`. | `migrations/0261…md` (Public API changes) |
| Snapshot re-bless directives: scrollbar glyphs (`migrations/0291…md` Consumer edits), right-aligned truncation ellipsis (`migrations/0281…md` Consumer edits) | as cited |
| "New code that needs its own modal geometry should take the authority rather than write the arithmetic" (`ModalSpec`/`modal_rect`) | `migrations/0323…md` (Consumer edits) |
| 0060 consumer edits: search `use termrock::{` and root type imports, move each under its module path, rebuild; "No runtime or logic change is required for this migration alone." | `migrations/0060…md:38-42` |

## Dead ends and contradictions

- **`runtime::RunOptions` is not a termrock API in jackin.** The task brief listed "runtime::RunOptions + runner usage" as used surface. rg: `termrock::runtime` has 0 hits in jackin; jackin's `RunOptions` is re-exported from `jackin_core` (`crates/jackin-docker/src/shell_runner.rs:20`). termrock's runtime docs (0092, 0280, 0289's runner parts) are therefore out of scope. HIGH.
- **`StatusRightGroup` does not exist** — 0 rg hits in jackin and no such symbol at termrock HEAD. The brief's usage list is wrong on this item. HIGH.
- **`keymap::glyph` false alarm.** At HEAD no free `pub fn glyph` exists in `keymap.rs`, which first read as a removal; it is a **const submodule** `keymap::glyph` (`crates/termrock/src/keymap.rs:566`, e.g. `glyph::LEFT_RIGHT`), present at both the pin and HEAD. jackin's `use termrock::{keymap::glyph, …}` (`crates/jackin-console/src/tui/components/footer_hints/common.rs:8`) is unaffected. HIGH.
- **Version labels vs reality.** Migration docs are tagged v0.12.0–v0.14.0, but the termrock workspace version at HEAD is still `0.11.0` (`Cargo.toml:13`) and git tags stop at `v0.9.0` (`git tag -l`). The "versions" are planned labels; adoption targets a git rev, not a published release. HIGH.
- **Grep mirage at HEAD:** `rg "pub struct Theme" crates/termrock/src/lib.rs` matches only gate-test strings asserting Theme's *absence* (`crates/termrock/src/lib.rs:123`); same for `PanelEmphasis` (`:139`) and the `ModalStack`/`FocusRing` re-export gates (`:66,166`). Symbol-presence greps on this repo must exclude `lib.rs` gate tests. HIGH.
- The brief's grep-pattern list (`DesignTokens`, `StatusBar`, `Theme`, `Role`) over-matches: on the order of 105–120 docs in range hit it depending on word-boundary handling (rg over `migrations/`), but most hits are incidental prose; the per-symbol grep set used here narrowed to the 33-doc table. Method note, HIGH for the over-match direction; the exact over-match count is pattern-sensitive.

## Open unknowns

- **Function-level signature diffs 0.11-pin → HEAD are not exhaustively audited.** Presence + migration-doc fate was verified per symbol; per-method parameter drift (e.g. `DialogScroll::handle_key` arity, `ListState` method set) was spot-checked only where a doc flagged it. A compile of jackin against HEAD is the decisive test. MED.
- **Undocumented drift:** 0331 claims every inventoried jackin usage "has a current public export or an explicit forward replacement" (`migrations/0331…md:17-18`) — upstream's claim, not independently proven for the full inventoried import surface beyond the presence checks above and the 40-doc table. MED.
- **Which jackin insta snapshots actually pin termrock-painted cells** (scrollbar glyphs, tab rules, ellipses, surface backgrounds) was not enumerated; both TUI crates carry insta and TESTING.md's snapshot-review gate makes the re-bless reviewable, but the blast radius in `.snap` files is unmeasured. LOW.
- **`DesignSystem::terminal_native()` vs the obsidian surface ladder** (0261): whether jackin's product design wants the new opaque surface backgrounds or the terminal-native opt-out is a product decision outside this chapter's scope; both paths verified to exist. Data only. HIGH on existence, decision open.
- The 5 class-bucketed doc ranges (258 docs after the second pass) were triaged by title + symbol grep, not full reads; individual docs could hide a one-line consumer edit for a used API. The first pass demonstrably missed 7 (caught by cross-checking chapter 01's compiler-measured breaks and a wider symbol re-grep); residual risk after the second pass is lower but non-zero — the compiler inventory (chapter 01) is the backstop for compile-visible breaks, behavioral sweeps remain grep-bounded. MED.
